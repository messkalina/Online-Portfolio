
import { Builder } from '@builder.io/react';

// Register your Builder.io components
export const registerBuilderComponents = () => {
  // You can register custom components here if needed
  // Example: Builder.registerComponent(MyComponent, { name: 'MyComponent' })
};

export default registerBuilderComponents;
